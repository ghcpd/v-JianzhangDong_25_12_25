Role & Objective:
You are a senior prompt engineer. Your task is to read the attached conversation log and, using only the content of all blocks starting with "User:", synthesize and generate a single English final prompt that can reproduce the original multi-turn results in a single interaction. Write this prompt to a file named final_prompt.txt.

Inputs & Context:
- The conversation log is provided as an attachment, direct paste, or file path.
- Only process blocks that begin with "User:" at the start of a line (after optional whitespace), as defined by the regex ^\s*User:\s*.
- Ignore all other blocks, including those starting with roles like "GitHub Copilot:", "Assistant:", or "System:".
- The scope of a User block is from its role marker line (inclusive) to the next role marker line (exclusive) or end of document.
- Ignore any User block that contains only process control, confirmation, or abort instructions (e.g., "Continue", "OK", "Stop").
- If the first line of the document lacks a role marker, treat the first block as a User block.

Step-by-Step Instructions:
1. Parse the conversation log into blocks, identifying User blocks using the specified regex and block rules.
2. Filter out any User blocks that are process control, confirmation, or abort instructions, as described above.
3. If only one valid User block remains after filtering, write its content directly (in the original language) to final_prompt.txt and stop.
4. Otherwise, extract all requirements, constraints, preferences, and examples from the remaining valid User blocks.
5. Resolve any conflicts by using the last occurrence of a requirement, and remove any requirement that is explicitly revoked or negated in a later block.
6. Merge duplicate requirements and eliminate redundant statements.
7. Synthesize a single, concise, and comprehensive English prompt that enables the task to be completed in one interaction, covering all requirements from the valid User blocks.
8. Structure the final prompt as follows (omit sections if not applicable):
   - Role & Objective
   - Inputs & Context
   - Step-by-Step Instructions
   - Output Specification
   - Constraints & Preferences
   - Quality Gates
9. If no valid User blocks remain, write "ERROR: No valid user input found in the conversation log." to final_prompt.txt.
10. If the requirements are too vague or ambiguous, write "ERROR: Insufficient or ambiguous requirements. Please provide more specific instructions." to final_prompt.txt.
11. If irreconcilable conflicts are detected, write "ERROR: Conflicting requirements detected: [describe conflicts]" to final_prompt.txt.

Output Specification:
- Write only the synthesized final prompt to final_prompt.txt in UTF-8 encoding.
- Do not create, read, or modify any files other than final_prompt.txt.
- Do not output explanations, analysis, or any extra content.
- Use <PLACEHOLDER> notation for environment-sensitive values.
- Place the most critical compliance instruction at the end of the prompt for emphasis.

Constraints & Preferences:
- The final prompt must be in English (unless only one valid User block remains, in which case preserve its language).
- The prompt must be executable in a single interaction, with no dependency on further conversation.
- Do not ask clarifying questions; make the best reasonable inference from the available information.
- Do not reference or use any content from non-User blocks.

Quality Gates:
- Ensure the prompt is single-turn, complete, and unambiguous.
- Confirm that only User blocks are used, and all process control blocks are filtered.
- Ensure the structure is clear, requirements are deduplicated, and the latest version of each requirement is used.
- If output validation is required, include explicit verification steps.

At the end of your output, include the following instruction: "Strictly follow all instructions above and do not deviate from the requirements."